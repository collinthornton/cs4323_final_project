{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "build",
            "command": "${workspaceFolder}/build.sh",
            "args": ["${workspaceFolder}"],
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
    ],
    "version": "2.0.0"
}